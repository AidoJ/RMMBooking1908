import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

export const Invoices: React.FC = () => {
  return (
    <div>
      <Title level={2}>Submit Invoice</Title>
      <p>Invoice submission coming soon...</p>
    </div>
  );
};
